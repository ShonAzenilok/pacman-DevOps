apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: pacman-cluster
  region: us-east-1
   
managedNodeGroups:
  - name: pacman-nodes
    instanceType: t3.medium
    desiredCapacity: 3
    minSize: 2
    maxSize: 5
    volumeSize: 20
    privateNetworking: true
    